# PocketLLM Portal MVP

Minimal working prototype implementing core features from Assignment 3.

## Quick Start

```bash
# Run the application
docker-compose up --build

# Access at http://localhost:3000
```

**Default admin**: username=`admin`, password=`admin123`
